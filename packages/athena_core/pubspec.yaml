name: athena_core
description: Athena agent core - UI-agnostic agent engine, domain models, and services.
version: 0.1.0
publish_to: none

environment:
  # 下限由 anthropic_sdk_dart 9.x 决定（要求 Dart >=3.12.0）；
  # CI / release / 本地开发统一 Flutter 3.47.1（Dart 3.13.1）。
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  anthropic_sdk_dart: ^9.0.0
  crypto: ^3.0.7
  http: ^1.1.0
  html: ^0.15.5
  logger: ^2.0.2+1
  meta: ^1.16.0
  openai_dart: ^8.1.0
  path: ^1.8.0
  signals: ^6.2.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
