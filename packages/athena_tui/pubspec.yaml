name: athena_tui
description: Athena terminal UI - nocterm-based chat client sharing the athena_core agent engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  athena_core:
    path: ../athena_core
  meta: ^1.16.0
  nocterm: ^0.8.0
  openai_dart:
    path: ../../third_party/openai_dart
  signals: ^6.2.0
  yaml: ^3.1.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
